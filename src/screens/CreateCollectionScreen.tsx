import { Text, View } from "react-native"

const CreateCollectionScreen = () => {
  return (
    <View style={{backgroundColor: 'blue'}}>
      <Text>CreateCollectionScreen</Text>
    </View>
  )
}

export default CreateCollectionScreen;